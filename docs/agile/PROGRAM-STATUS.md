# Program status — pointeur de reprise

> **But.** Fichier de reprise unique. Quand une session redémarre (gestion du contexte), dis
> simplement **« continue la prochaine tâche »** ou lance **`/next-task`** : l'assistant lit ce
> fichier + le plan actif, vérifie l'état git, puis enchaîne.
>
> **🟢 Programme ACTIF — Phase 2 (`docs/agile/ROADMAP-PHASE-2.md`), curseur sur EPIC 7 · sous-tâche 7.1** (frontend Order e-Transfer — tunnel checkout + admin « marquer payé » + i18n + e2e) — reprendre sur branche **fraîche depuis `master` mis à jour** (`feat/epic-7-paiement-etransfer` déjà créée, à rebaser/réutiliser depuis `229730e`).
> **Ordre Phase 2 restant : ~~9~~ ~~10~~ ~~13~~ ~~14~~ ~~11~~ ~~8~~ → 7 (spike mergé `6de4721` ; 7.0 backend mergé `229730e` ; → 7.1 frontend PROCHAIN).**
>
> **✅ PROGRAMME HORS-ROADMAP — « Abris toujours configurés avant le panier » MERGÉ (2026-06-23, PR #58, SHA merge `229730e`), branche `feat/shelters-configure-only` supprimée.** Demande directe du propriétaire : un abri ne doit **JAMAIS** pouvoir être ajouté au panier sans dimensions (parité abristempo.com — aucun SKU d'abri sans dimensions). Suppression du double système (produits-abris à prix fixe **retirés**) ; **tous** les abris sont des `ShelterModel` paramétriques configurés via un **modal** (accueil + boutique + fiche). 6 phases, **1 branche**, commit local par phase, revue indépendante `code-reviewer` à chaque frontière + **revue finale holistique APPROVE WITH NITS** (invariant « zéro abri sans dimensions » prouvé étanche sur les 2 tiers).
> - **P1** Tous les types d'abris paramétriques + **nouvelle catégorie `abris-monopente`** (le monopente quitte « simples » → Simples = exactement 3 modèles) + 5 nouveaux modèles (rangement/entrée-passage/industriel). `EnsureCategoriesAsync` idempotent (L-049). **P2-3** Location portée sur les modèles (`MonthlyRentalCents`, `RentalContract.CreateForModel`, `GET /shelters/rentable`, `ShelterSizeRules` partagée panier+location L-004) + migration `RentalsOntoShelterModels`. **P4** Retrait des 8 produits-abris (soft-delete idempotent) + catalogue installation re-sourcé sur les modèles. **P5** Configuration obligatoire partout : accueil héberge l'overlay (supprime le **404** `abri-pente-unique`), catalogue « Tous » mixte, fiche via résolveur `GET /catalog/{slug}/type` (1 appel, fin du 404), Location refondue (sélecteur de taille APG). **P6** nettoyage SVG + imagerie cartes catégorie, **garde de régression e2e** `shelter-configure-required` (accueil + boutique + « Tous » + sombre, **non-vacueuse** L-005) + garde a11y dual-thème/APG, docs, leçons **L-049→L-052**.
> - **Gates finaux** : `dotnet test` **451 unit + 142 IT / 0** ✅ · `npm test` **412** ✅ · `npm run e2e` **158/0** ✅ · `npm run build`/`build:i18n` bilingue ✅ · round-trips LocalDB (modèles par catégorie, location sur modèle, 8 abris `IsDeleted=1`) ✅. CI GitHub Actions 100% verte (Backend + Frontend + Build & Deploy + SonarCloud) ✅. Hors périmètre (reportés) : **accessoires** dans le modal (ancrages…) ; nits pré-existants EPIC 7 (`AdminOrderDto` TS sans `paymentReference`, 15 orphelins EN déjà sur `master`).
>
> **⭐ CADRAGE MVP figé (propriétaire, 2026-06-22)** : e-Transfer manuel = **VRAI moyen de paiement de PRODUCTION** (PAS une démo ; aucun wording démo) ; **cartes hors périmètre**. **Les 3 flux** (commandes/locations/installations). **US-7.1 + US-7.3** (port + adaptateur défaut keyless `ManualInteracPaymentService` + stubs keyless VoPay/Paysafe non activés + patron webhook **documenté non codé**). **Statut porté par l'agrégat** (owned VO `PaymentInfo`, PAS d'entité `Payment`). **Locations : `RentalStatus.PendingPayment → Active`**. Réf **base32 ~12 car. non devinable**. `Payments:Manual:RecipientEmail` = placeholder dev → **vrai courriel marchand requis avant prod** (Key Vault).
> **Découpage (~13 pts, plan architecte)** : **7.0** port + Order backend (✅ FAIT) → **7.1** frontend Order (tunnel checkout : RETIRER les champs carte simulés + étape e-Transfer ; admin « marquer payé » ; i18n ; e2e dual-thème) → **7.2** Locations (PendingPayment→Active) → **7.3** Installations → **7.4** stubs VoPay/Paysafe + doc webhook. **Revue indépendante complète à la fin de 7.1** (tranche verticale prouvée) puis fin d'épic.
> **✅ 7.0 LIVRÉ (commit local `abc1eb4`, branche `feat/epic-7-paiement-etransfer`)** : `PaymentInfo` VO + `Order.AttachPaymentReference`/`MarkPaid` (réutilise `Confirm()`, idempotent L-046) ; port `IPaymentService` + `IPaymentReferenceGenerator` ; `Base32PaymentReferenceGenerator` (aléa crypto) ; `ManualInteracPaymentService` (défaut keyless) + `PaymentsOptions` + `switch Payments:Provider` en DI ; `PlaceOrder`→`PlaceOrderResult{id,payment}` ; `ConfirmOrderPaymentCommand` (AdminOnly) ; migration `20260623002950_AddOrderPaymentInfo` (2 colonnes nullables) ; `AdminOrderDto` expose réf+confirmé-le. Revue intra-cycle code-reviewer APPROVE WITH NITS (rename `customerEmail`, garde `ConfirmedAt`). **Gates** : `dotnet test` **430 unit + 130 IT / 0** ✅ · migration appliquée + **round-trip live LocalDB** (réf `ABR-…`, `Pending→Confirmed`, **401 anon / 204 admin**, **2e confirm → 422** idempotence) ✅. (Backend seul — frontend = 7.1.)
> **PROCHAIN : sous-tâche 7.1** (frontend Order) sur la branche `feat/epic-7-paiement-etransfer` via `feature-developer`, puis **revue indépendante complète** (frontière de tranche verticale).
>
> **EPIC 7 · US-7.0 — SPIKE PAIEMENTS LIVRÉ (2026-06-22), MERGÉ `6de4721` (PR #56), branche `docs/epic-7-payments-spike` supprimée.** Décision utilisateur au lancement : **spike d'abord** (US-7.x = « Could »). Doc : `docs/spikes/epic-7-payments-spike.md` (gabarit spike EPIC 15, recherche 2026 + ancrages code réels vérifiés).
> **Conclusion** : seule voie **gratuite ET sans clé** = **e-Transfer MANUEL à dépôt auto** (réf. commande → virement client → réconciliation admin, zéro donnée bancaire stockée) → **adaptateur par défaut** `ManualInteracPaymentService`, calqué sur `PhotonPlacesService`. **VoPay/Paysafe/Payment Source = contrats PAYANTS → REJETÉS** par `budget-free-tier.md` (laissés en **stubs keyless** documentés). Interac = réseau fermé → tout rail direct exige un intermédiaire réglementé payant.
> **Reco** : MVP US-7.1 manuel (~8 pts), **statut de paiement porté par les agrégats existants** (`Order.Confirm()` : `Pending→Confirmed`, garde déjà présente) — **PAS** d'entité `Payment` lourde ; webhooks signés/idempotents = **patron documenté, non codé** ; US-7.2 (Interac Debit/AccèsD) + cartes = **gelés** (payant, accord budget requis).
> Livrable documentaire (aucun code). **Décisions §7 TRANCHÉES par le propriétaire (2026-06-22)** : voir le « CADRAGE MVP figé » en tête → MVP manuel **+ stubs API** ; **les 3 flux** ; **statut sur agrégat** (pas d'entité `Payment`) ; **vrai paiement production** (pas démo). Implémentation lancée (cf. bloc MVP en tête).
> **EPIC 8 · US-8.1 — Module de paie informatif — MERGÉ (2026-06-22), branche `feat/epic-8-paie` · PR #55, SHA merge `7716216`.**
> Module **informatif** admin de paie : taux horaire `HourlyRate` (decimal nullable CAD) sur `AppUser`, statut `PayStatus` (`À payer`/`Payée`) sur `WorkHoursEntry`, query CQRS `GetPayrollSummary` (heures totales × taux, total masse salariale par fenêtre de dates), commandes `SetEmployeeHourlyRate` + `MarkEntriesAsPaid` (AdminOnly), `PayrollController` (`/api/v1/payroll`), vue admin `/admin/paie` accessible (tableau sémantique, sélecteur de période, édition taux inline, action « marquer payé » — focus retourné après action L-006).
> **Hors périmètre** : aucun calcul fiscal/déduction, aucun virement. US-8.2 « versement réel » reste **Won't now** (dépend EPIC 7).
> **Décisions propriétaire figées** : taux sur `AppUser` (pas d'entité Employee) ; statut par jour sur `WorkHoursEntry` (pas d'entité période de paie).
> **Migration** `AddPayrollHourlyRateAndPayStatus` : `HourlyRate decimal(18,2) NULL` + `PayStatus nvarchar(20) DEFAULT 'APayer'`. `MigrateOnStartup` reste OFF (L-022).
> **Revue indépendante `code-reviewer` + `solid-review` : APPROVE WITH NITS** — 1 Minor L-006 (focus après « marquer payé ») corrigé pendant l'implémentation ; 2 nits corrigés commit `f3f6ad8` : retrait `GetHourlyRateAsync` mort (dead code / ISP) + commentaire e2e trompeur. Aucun bug de correction, aucune fuite Clean Architecture, aucun trou a11y.
> **Coût** : zéro dépendance externe, zéro clé API — règle `budget-free-tier.md` respectée.
> **Gates** : `dotnet test` **422 unit / 0 échec** ✅ · `dotnet test` **IntegrationTest 126 / 0** ✅ · round-trip live LocalDB (set rate 22.50 → 204, summary amount 202.50, mark-paid → Payée, persistance vérifiée) ✅ · `npm run build` ✅ · `npm test` **396/396** ✅ · `npm run e2e` admin-payroll **6/6** (axe dual-thème + clavier + barrière réseau, non-vacueux) ✅ · `npm run build:prod` bilingue, i18n 41 ids symétriques fr/en ✅.
>
> **EPIC 11 · US-11.3 — Optimisation de tournée admin — LIVRÉ (2026-06-22), branche `feat/epic-11-calendrier` · CLÔTURE EPIC 11.**
> lat/lng stockées sur `BookingSlot` (migration `AddBookingSlotCoordinates`), géocodage Photon keyless via `IPlacesService.GeocodeAsync`.
> `RouteOptimizer` maison (nearest-neighbour + Haversine, zéro dépendance) ; `OptimizeRouteCommand` + `POST /planning/optimize` (AdminOnly).
> Réécrit `SlotStart` Pending+Confirmed, anti-collision `SlotRules.Overlaps`, surplus/exclus non recalés.
> Frontend calendrier : bouton Admin, live-region (L-027), heures fuseau local (L-044), i18n fr/en symétrique.
> **Revue indépendante `code-reviewer` : APPROVE WITH NITS** — 1 Minor collision (chemin parallèle sans garde `Overlaps`) + 1 Nit N+1 corrigés. **Leçon L-046** capturée (invariant non-chevauchement présent dans un chemin d'écriture, absent du chemin parallèle — vérifier TOUS les points d'entrée).
> **Gates** : `dotnet test` **403 unit + 119 IT / 0 échec** ✅ · migration appliquée + round-trip live LocalDB (lat/lng Photon, SlotStart réécrits 08:00Z/10:00Z, total 7.20 km) ✅ · `npm run build` ✅ · `npm test` **387/387** ✅ · `npm run build:prod` bilingue ✅ · `npm run e2e` admin-calendar **13/13** (optimize + dual-thème axe + `timezoneId: 'America/Toronto'` 12:00Z→08:00 L-044, non-vacueux) ✅.
> **EPIC 11 · US-11.2 partie 2 — Ajout de RDV depuis le calendrier admin — LIVRÉ (commit local, 2026-06-22), branche `feat/epic-11-calendrier`.**
> Sous-formulaire « ajouter un RDV » dans l'overlay détail-jour (Admin only) : contact libre (compte express) OU client existant via recherche.
> Réutilise `CreateBooking` + `available-slots` (créneau UTC brut, L-044), **zéro migration**.
> Query CQRS `SearchCustomers` via `IIdentityService` (DIP) ; attribution `CustomerId` sécurisée (garde `isStaff`, non-staff ignoré, L-028).
> Radiogroups APG (L-015), focus cible inconditionnelle (L-006 corollaire B), 36 ids i18n symétriques fr/en (L-042).
> **Revue indépendante `code-reviewer` : APPROVE WITH NITS** — 1 Minor focus corrigé ; **leçon L-006 enrichie** d'un corollaire B (retour-focus inconditionnel vers le champ déclencheur même sans suppression de DOM).
> **Gates** : `dotnet test` **506 / 0 échec** ✅ · round-trip live LocalDB (attribution vérifiée) ✅ · `npm test` **384/384** ✅ · `npm run e2e` admin-calendar **12/12** ✅ · `build:prod` bilingue ✅.
> **Prochain** : US-11.3 optimisation de tournée (heuristique maison `GeoDistance` ou OpenRouteService proxy), puis **clôture EPIC 11 + PR**.
> **EPIC 11 · US-11.2 partie 1 — Overlay détail du jour + saisie des heures employé — LIVRÉ (commit local, 2026-06-22), branche `feat/epic-11-calendrier`.**
> Cliquer un jour ouvre un `role="dialog"` (focus/Échap/retour-focus APG, mêmes mécanismes que le panneau RDV
> US-11.1) affichant le détail du jour : liste des RDV + **tous les employés Staff** avec leurs heures de travail.
> **Admin** : formulaire saisie/édition heures par employé (End ≤ Start → 422) ; **Staff** : lecture seule.
> **Entité** `WorkHoursEntry` — regular entity (non OwnsMany — L-035), minutes-from-midnight en heure **locale**
> (L-044), `ISoftDeletable` + index unique filtré `(EmployeeId, WorkDate)` `HasFilter("[IsDeleted] = 0")`
> (L-045, idiome `Product`/`ShelterModel`), FK `EmployeeId → AppUser` Restrict.
> **Migration** `20260622052312_AddWorkHoursEntry` (MigrateOnStartup reste OFF — L-022) ; appliquée + round-trip
> live LocalDB (upsert persist, même clé → UPDATE, End ≤ Start → 422). **CQRS** : `GetDayDetailQuery` (read,
> `StaffOrAbove`) + `UpsertWorkHoursCommand` (write, `AdminOnly`) ; `IIdentityService.GetStaffMembersAsync`
> (Application ne touche pas `AppUser` — DIP). **Contrôleur** : `PlanningController`
> `GET /api/v1/planning/day` + `PUT /api/v1/planning/work-hours`. **Frontend** : planning model/service,
> rework dialogue calendrier, util pur `work-hours.util`, 17 ids i18n **symétriques** fr/en.
> **Revue indépendante `code-reviewer` + `solid-review` : APPROVE WITH NITS** — 1 Minor corrigé (index unique
> `HasFilter("[IsDeleted]=0")` manquant → ajouté, migration régénérée) ; 2 nits acceptés. **Leçon L-045**
> capturée (`ISoftDeletable` + index unique → toujours `HasFilter`).
> **Gates** : `dotnet test` **383 unit + 110 IT / 0 échec** ✅ · migration appliquée + live LocalDB ✅ ·
> `npm run build:prod` (bilingue) ✅ · `npm test` **378/378** ✅ · `npm run e2e` admin-calendar **10/10**
> (APG dialog + dual-theme axe admin surface + `timezoneId: 'America/Toronto'` 12:00Z→08:00 L-044) ✅ · i18n symétrique ✅.
> **Prochain** : ajout de RDV/employé sur le calendrier (puis optimisation de tournée US-11.3). **Commit LOCAL
> uniquement** (sous-tâche intermédiaire — pas de PR/merge tant que l'épic n'est pas complet, cf. [[program-git-flow]]).
> **EPIC 11 · US-11.1 — Vue calendrier LECTURE SEULE — LIVRÉ (commit local, 2026-06-22), branche `feat/epic-11-calendrier`.**
> Calendrier `/planning` (mois/semaine/jour) accessible qui **agrège les `BookingSlot` existants** (zéro
> nouveau mécanisme, **zéro migration**, zéro entité). **Décisions propriétaire figées** : (1) lecture seule
> stricte ; (2) employé = `AppUser` rôle Staff ; (3) **Admin ET Staff voient TOUT** (pas de filtre par
> utilisateur — le filtrage « par installateur assigné » est reporté à un sous-épic ultérieur qui ajoutera le
> FK + migration). **Backend** : query CQRS `GetCalendarBookings` (fenêtre `from/to`, filtre L-007 commenté +
> pinné, `internal sealed` auto-Scrutor) + endpoint `GET /bookings/calendar` `[Authorize(StaffOrAbove)]` (401
> anon / 403 Customer / 200 Staff+Admin / 422 plage invalide). **Frontend** : route `/planning` **hors `/admin`**
> + `staffGuard` (sinon adminGuard fermerait au Staff), composant grille **APG maison** (roving tabindex +
> flèches/Home/End/PageUp/PageDown — L-015), bascule de vue `radiogroup`, panneau RDV-jour lecture seule (focus
> + Échap + retour focus L-006), util **pur** `calendar-grid.util`, lien navbar `@if(isStaff())` + carte
> dashboard, i18n fr/en **symétrique** (35 ids calendrier + `navbar.planning`). **Revue indépendante
> `code-reviewer` + `solid-review` : APPROVE WITH NITS** — 1 **Major corrigé** (heure panneau rendue en **UTC**
> au lieu du **fuseau local** comme le reste de l'app `bookings`/`installation` ET comme le regroupement local
> du composant → décalage inter-écrans 4-5 h en EDT ; corrigé en TZ locale + **test e2e à `timezoneId` forcé**
> qui prouve 12:00Z→« 08:00 » ; **leçon L-044** capturée, axe fuseau de L-004). **4 défauts attrapés aux gates**
> (run dev coupé avant gates) : e2e manquant (écrit) ; **contraste** `.cal__view-option--active`/pastille =
> blanc sur `--color-primary` qui bascule (2.76:1 sombre) → jeton marque-FIXE `--color-red-600` (L-023/L-033) ;
> stub `navbar.spec` sans `isStaff` (crash template) ; **fuite de `Date` faké** dans `calendar.spec` (pas
> d'`afterEach(useRealTimers)`) contaminant des specs sœurs. **Gates** : `dotnet test` **468** (365 unit + 103
> IT) ✅ · `npm run build`/`build:prod` ✅ (budget initial **505.67 kB** > 500 kB **WARN** pré-existant EPIC 13/14,
> < 1 MB error → CI OK ; +0.77 kB navbar/route) · `npm test` **370/370** ✅ · `npm run e2e` admin-calendar **8/8**
> (APG clavier + accès Customer refusé + heure locale + axe **dual-thème**) ✅ · i18n symétrique ✅.
> **Nits/Minor reportés (non bloquants, APPROVE)** : (a) Minor — grille mois = 42 `gridcell` dans **une seule**
> `role="row"` (sémantique ARIA aplatie ; clavier + axe OK, lecteur d'écran annonce « 1/42 ») → restructurer en
> rows hebdo dans un sous-épic ; (b) Nit fixtures hors heures d'affaires. **Pré-existant hors périmètre** :
> orphelins EN `messages.en.xlf` (home.catalog.*, cart.orderError…) déjà sur master.
> **EPIC 14 MERGÉ (2026-06-21)** — PR #52, SHA merge `fd8083f`.
> **EPIC 14 TERMINÉ (2026-06-21)** — branche `feat/epic-14-carte-precise` : carte `/mesurer` plus précise,
> **100 % gratuit** (source HD payante **écartée** — règle budget). **US-14.1** over-zoom Esri :
> `SATELLITE_MAX_NATIVE_ZOOM=19` + `SATELLITE_MAX_ZOOM=21` sur le tileLayer + zoom localisé 20→21
> (`tile-provider.const.ts`, `map-measure.ts`). **US-14.2** corrige **L-034** : nouvel util **pur**
> `measure-rect.util` mesure **par arête** (haversine hand-roll, pas de `@turf/distance`), apparie côtés
> opposés ; `handleShape` l'essaie d'abord et **retombe sur `turf.bbox`** pour un polygone libre
> (non-régression exacte). Test pivoté 45° **non-vacueux** (prouve le rejet du gonflement bbox). Revue
> indép. `code-reviewer` **APPROVE** (0 Critical/Major/Minor ; nit JSDoc « quadrilatère » appliqué).
> Gates : `npm run build` ✅ · `npm test` **345/345** ✅ · `npm run e2e` mesurer **12/12** (3 voies + geoman
> dessinable + axe dual-thème) ✅ · `build:prod` ✅. **⚠️ Dette-perf** : bundle initial **504.90 kB > 500 kB
> (WARN)** — **pré-existant** (EPIC 13 n'avait lancé que le build dev, jamais `build:prod`), **sous le seuil
> error 1 MB → CI ne casse pas** ; à trancher séparément (rogner l'initial ou relever le budget warn ~520 kB).
> Pas de nouvelle leçon (L-034 déjà documentée, correctement appliquée).
> **EPIC 13 TERMINÉ (2026-06-21)** : 4 commits sur `feat/epic-13-mesurer-rework` (`ca247f3` 13.1 →
> `68c6b82` 13.2 → `e5c262c` 13.3 → `e000f59` nits) — refonte `/mesurer` « Trouver mon abri » :
> stepper **inversé** Adresse→Mesure→Résultats ⇒ **1. Dimensionner (radiogroup APG 3 voies : je connais
> mes dimensions / par véhicules / mesurer sur carte) → 2. Conseil** ; adresse demandée **uniquement**
> dans la voie carte (composant `map-voie`, input + carte sur la même page, pré-rempli/auto-centré si
> connecté) ; `results-step`→`conseil-step` (contrat `/shelters/suggest` inchangé). Voie « manuel » du
> calculateur promue en `known-dimensions`. Revue indépendante **APPROVE WITH NITS** (zéro Critical/Major ;
> nits `new FormBuilder()`→`inject()` corrigés) ; **L-042** (target EN figé sur `@@id` réutilisé) +
> **L-043** (`effect` sur source async) capturées. Gates : `npm run build` ✅ · `npm test` **338/338** ✅ ·
> `npm run e2e` **130/130** (3 voies + contraste dual-thème non-vacueux) ✅ · i18n 793 symétrique ✅.
> **PR #51 MERGÉE (`9d3f2a4`)**, CI verte, prod auto-déployée (2026-06-21).
> **EPIC 10 TERMINÉ (2026-06-21)** : 9 commits sur `feat/epic-10-smart-suggestion` —
> US-10.1 (suggestion par catégorie-qui-rentre, endpoint `/shelters/suggest`, deep-link configurateur,
> retrait ancien `suggest-shelters` Product) + US-10.2 (orientation véhicules côte à côte / derrière,
> noyau `footprint` neutre L-041) — revue indépendante APPROVE WITH NITS, Minor L-028 fermé,
> L-040/L-041 capturées, PR #50 MERGÉE (`30f4b41`).
> Ordre Phase 2 restant : ~~9~~ ~~10~~ ~~13~~ ~~14~~ ~~11~~ → **8** → 7.
> **Programme G terminé (A→H) :** `C:\Users\phili\.claude\plans\majestic-soaring-sprout.md`
> **Programme initial terminé (A→F) :** `C:\Users\phili\.claude\plans\1-i-want-you-glistening-barto.md`
> Maintiens ce pointeur à jour à la fin de chaque sous-tâche (c'est ce que l'assistant relit).
>
> **🗂️ Phase 2 planifiée — `docs/agile/ROADMAP-PHASE-2.md`.** Épopées issues de **deux** demandes :
> `probleme abris-auto-outaouais.docx` (2026-06-16 → EPIC 7–12) **et `… (1).docx` (2026-06-17 →
> EPIC 13–16 + affinages)**. EPIC 7 paiements (Interac), 8 employés/paie, 9 catalogue par dimensions,
> 10 suggestion intelligente, 11 calendrier, 12 contraste formulaires, **13 refonte `/mesurer`
> (ordre + adresse optionnelle), 14 carte plus précise (zoom), 15 champ d'adresse unifié (spike→reco),
> 16 doc d'architecture + briques manquantes**. Ordre conseillé : 12(reste) → 15 → 9 → 10 → 13 → 14
> → 11 → 8 → 7. User stories EPIC 7→16 dans `product-backlog.md`.
>
> **⚙️ Quick wins livrés 2026-06-17 (suite au 2ᵉ .docx), branche `docs/docx-followup-planning-and-epic12` :**
> (1) **PR #41** « tests frontend » : vérifié = **flake** e2e post-merge (`mesurer.spec.ts:118` smoke
> carte, L-019), pas une régression — **re-run CI vert** (`27658589841`) ; (2) **EPIC 12 partie 1** :
> correctif « blanc sur blanc » au focus (register/login/reset : `background:white` codé en dur →
> jeton `--color-surface`), gardé par `e2e/auth-input-contrast.spec.ts` (ratio direct, non vacueux ;
> audit §5.11) ; (3) **EPIC 16 diagramme** : `docs/architecture/system-design.{md,drawio}` (flux
> client→couches→BD + briques manquantes cache/cookies/LB…).
>
> **✅ EPIC 12 (contraste formulaires/focus) — TERMINÉ (2026-06-18), branche `fix/epic-12-p2-contrast-badge-tab` :**
> partie 2 livrée. L'inventaire a confirmé qu'aucune feuille scopée sœur ne reproduisait le bug L-032
> (la partie 1 avait corrigé les 2 seuls cas auth/reset). Corrigés **au niveau jeton** (jetons
> marque-fixes non surchargés en sombre) **4 surfaces** en échec AA en thème sombre : badge « Ajusté
> serré » `/mesurer` (1.67→5.02:1), onglet `.profile-tab.is-active` (2.77→6.47:1), et **2 trouvées en
> revue indépendante** — `.booking__slot--selected` (installation) + `.mesurer__step--current
> .mesurer__step-num` (2.77→≥4.5:1). Gardés par 3 e2e dual-thème non vacueux (`badge-tab-contrast`,
> `form-focus-contrast`, `primary-surface-contrast` + helpers `e2e/support/contrast.ts`). **Leçon
> L-033** capturée (un fix de contraste au jeton n'est clos qu'après un grep de TOUS les consommateurs
> du motif fautif).
>
> **✅ EPIC 15 — SPIKE (US-15.1) TERMINÉ (2026-06-18), branche `docs/epic-15-address-spike` :**
> `docs/spikes/epic-15-address-field-spike.md`. Comparé *un seul champ* vs *champs séparés* et *lecture
> seule* vs *éditable* (GOV.UK, WCAG 2.2 SC 1.3.5, Baymard, web.dev), ancré dans le code réel.
> **Décision utilisateur :** (1) **un seul champ « n°+rue »** (= `address-line1`, supprime le contrôle
> `civicNumber` séparé → corrige autocomplete cassée + civique désynchronisé *par construction*) ;
> (2) **auto-rempli mais ÉDITABLE** (« lecture seule » du `.docx` **écarté** : le lookup renvoie parfois
> un mauvais CP, « 45 Atmosphère → J9A 2V9 », qui doit rester corrigeable) ; (3) **tokens WCAG 1.3.5**
> partout ; voie serveur **B1** (DTO découpé, split à la présentation). **Implémentation US-15.2/15.3
> différée** (« s'arrêter au spike »). **Prochaine Phase 2 = EPIC 9** (catalogue par dimensions) → 10
> → 13 → 14 → 11 → 8 → 7 — *ou* la refonte US-15.2/15.3 si l'utilisateur la relance, via `/feature-cycle`.

---

## 🟢 Programme G — curseur ACTIF

> Plan : `majestic-soaring-sprout.md`. Ordre : A (contraste) → B (curseur) → C (langue locale) →
> D (adresse) → E (premier utilisateur) → F (invité) → G (catalogue/fit) → H (déploiement manuel).
> Flow par sous-tâche : `/feature-cycle` (architect→dev→reviewer→mentor), commit local ; fin d'épic :
> review → PR → CI verte → merge `master`.

| Épic | Sujet | État |
|------|-------|------|
| **A** | Contraste & lisibilité boutons (`.btn--primary` texte invisible en sombre) | **✅ MERGÉ — PR #31 (`3e08e68`), CI verte, prod auto-déployée** |
| **B** | Curseur custom : point + anneau magnétique | **✅ MERGÉ — PR #32 (`56e7409`), CI verte, prod auto-déployée** |
| **C** | Bascule langue (EN) en local (serveur localisé dev) | **✅ MERGÉ — PR #33 (`a4ad2d2`), CI verte, prod auto-déployée** |
| D | Système d'adresse unifié (champ unique intelligent) + zone 100 km + **2 choix adresse connecté (pastille profil OU autre adresse)** | **✅ MERGÉ — PR #34 (`9c91254`), CI verte, prod auto-déployée** |
| E | Parcours premier utilisateur (register→profil ; alerte adresse) | **✅ MERGÉ — PR #37 (`233a388`), CI verte, prod auto-déployée** |
| F | Accès invité (compte express silencieux à la confirmation) | **✅ MERGÉ — PR #38 (`e536cd2`), 3 gates code verts ; prod en attente re-run MCR** |
| G | Catalogue marques/modèles + dimensions (installation & fit mesurer) | **✅ MERGÉ — PR #39 (`ed5f82e`), CI verte, prod auto-déployée** |
| **H** | Déploiement (région SWA + backend Azure) — **manuel utilisateur** | **✅ FAIT (2026-06-16) — backend en ligne (Container Apps + Azure SQL S0), provisionné par Terraform `infra/`, image API déployée et vérifiée (`GET /api/v1/products` → 200), PR #40** |

**Décisions utilisateur figées** : adresse = **champ unique intelligent** → champs structurés
éditables ; curseur = **point + anneau magnétique** ; invité = **compte express silencieux**
(clé courriel, sans mot de passe) créé **à la confirmation**, coordonnées+adresse saisies seulement
à ce moment → `CustomerId` réel partout.

---

## Curseur courant (Programme G)

- **✅ Épic H — Déploiement backend Azure : FAIT (2026-06-16) — PR #40 (`feat/epic-h-deploy`).**
  - **Livré** : module **Terraform `infra/`** (RG + Azure SQL S0 + ACR + Container Apps, identité managée **user-assigned** + AcrPull) ; image API .NET 10 **buildée par le SDK** (`dotnet publish /t:PublishContainer`) + poussée dans ACR ; Container App déployée et **vérifiée live** (`GET https://abristempo-api.livelyforest-90718d07.canadacentral.azurecontainerapps.io/api/v1/products` → 200, migration EF + seeding OK) ; `environment.prod.ts apiUrl` substitué au FQDN réel ; workflow CD corrigé + `infra/deploy-backend.ps1`.
  - **Contraintes abonnement étudiant rencontrées (importantes pour la suite)** : (1) **ACR Tasks DÉSACTIVÉ** (`TasksOperationsNotAllowed`) → `az acr build` impossible **et** le workflow GitHub d'origine inopérant → bascule sur **build SDK .NET sans Docker** (workflow réécrit `dotnet publish /t:PublishContainer`, gated `AZURE_CREDENTIALS`) ; (2) `registry { identity = "System" }` faisait **expirer** la révision → fix = identité managée **user-assigned** créée avant l'app (AcrPull pré-attribué) ; (3) image d'amorçage devait écouter sur **8080** (la `helloworld` écoute 80 → sonde KO) → `mcr.microsoft.com/dotnet/samples:aspnetapp` ; (4) **admin ACR activé** uniquement pour le PUSH (le pull runtime reste via l'identité managée). **CD automatique non câblé** : pas de service principal (tenant Cégep peut le bloquer) → redéploiements via **`infra/deploy-backend.ps1`** (manuel, reproductible). SWA frontend laissé en **Central US** (un SWA sert depuis l'edge mondial — la région n'affecte pas la latence ; Canada n'est pas une région SWA).
  - **Reste optionnel** : si un service principal devient disponible → `gh secret set AZURE_CREDENTIALS` active le CD auto (workflow déjà prêt). Sinon, `deploy-backend.ps1` pour chaque redéploiement backend.
  - **Diagnostic clos (e2e PR #39)** : l'unique rouge CI était un **flake d'hydratation SSR pré-existant** (L-012), PAS une régression d'Épic G. `e2e/address-autocomplete.spec.ts:211` faisait un `fill('77')` one-shot sur `#civicNumber` avant l'hydratation du CVA → modèle de form vide → la suggestion réécrivait `''` (assertion recevait `''`, contrôle `ng-pristine`). Spec byte-identique à `master`, aucune touche G sur la cascade `/location` (la théorie git-ops « mock shelter-catalog » était bien fausse). Fix = `fill` enveloppé dans `expect(...).toPass()` (commit `1eb1321`), **L-012 affûtée** (corollaire `fill()` one-shot). CI repassée 100% verte → merge.

- **✅ Épic G — MERGÉ vers `master` (2026-06-16) — PR #39 (`ed5f82e`), branche `feat/epic-g-catalog` supprimée, CI verte, prod auto-déployée (Build + Deploy verts, pas de 429 MCR). (détail des 3 sous-tâches ci-dessous.)**
  - **✅ G1 — Catalogue marques/modèles sur `Product` : commit local `d19512f`.** `Brand`/`Model`
    (`string?` nullable, idiome **miroir de `BookingSlot.Brand/Model`** — pas d'entité séparée) ajoutés
    au domaine (`Product.Create` params en fin + `SetBrandModel`), config EF `nvarchar(100)`, migration
    **`AddProductBrandModel`** (`20260616154411`, appliquée LocalDB ; `MigrateOnStartup` reste OFF L-022).
    Propagation commands/handlers/validators (`MaximumLength(100)`, **optionnels** — non-breaking, défaut
    `null` en fin de record) + projections `ProductDto` (`GetAll`/`GetBySlug`). `ProductSeeder` enrichi
    (marque « Abris Tempo » + modèles sur les 8 abris réels ; toiles/accessoires `null` ; idempotence
    `AnyAsync` inchangée). Sync `product.model.ts` (3 interfaces) + `admin/products.ts` payloads + 4 specs.
    Tests handler Create (avec/sans, trim) + Update (set/clear, **nouveau** fichier). **Gates** :
    `dotnet test` **319/0** (Products filter 48/0) ✅ · migration appliquée + **round-trip live LocalDB**
    (POST trim `"  Abris Tempo  "`→`"Abris Tempo"`, PUT→null OK, L-001) ✅ · `npm run build` typecheck ✅ ·
    vitest specs affectés **21/21** (axe inclus) ✅. *Nota : colonnes nullable → pas de backfill ; les
    produits déjà semés restent `null` jusqu'au prochain seed sur base vide ou POST/PUT.*
  - **✅ G2 — Installation : selects marque → modèle → dimensions (endpoint `GET /api/v1/products/shelter-catalog`) : commit `443afce`.** Endpoint `[AllowAnonymous]` + route littérale **avant** `{slug}` (verrouillé par 2 IT) ; `GetShelterCatalogQuery`/handler/DTOs groupés par marque (`.ThenBy(Slug)` déterministe — **L-030**) ; frontend `features/installation` : selects marque→modèle liés par signal, dimensions en lecture seule (repli catalogue vide, garde `excludedBrand` conservée) ; service/modèles TS ; 9 ids i18n **symétriques** fr/en.
  - **✅ G3 — Mesurer : carte agrandie + backfill seeder + `Brand/Model` dans résultats : commit `fd693c2`.** Carte `/mesurer` breakout CSS hors `.container--narrow` + `ResizeObserver`→`invalidateSize` (teardown `DestroyRef`, SSR-safe) ; **cause racine du fit cassé = base dev périmée** : seeder idempotent n'avait jamais rétro-rempli `Brand/Model` sur les produits existants → **backfill idempotent par slug/par champ** (préserve saisie admin, gardé par `ProductSeederBackfillTests`) ; `Brand`/`Model` exposés dans `ShelterSuggestionDto` + projection + affichage `results-step`.
  - **Revue de frontière indépendante (`code-reviewer` + `solid-review` backend) : APPROVE** — 10 points vérifiés, zéro bloquant.
  - **Leçons capturées :** L-030 (`.ThenBy` + `.First()` déterministe sur tri non-unique) · L-031 (seeder idempotent + colonne tardive = backfill par champ + garde CI).
  - **Gates finales :** `dotnet test` **328/0** (250 unit + 78 IT) ✅ · `npm run build` ✅ · `npm test` **262/262** ✅ · `npm run e2e` mesurer 6/6 + installation 2/2 ✅ · i18n symétrique ✅ · round-trip live LocalDB (catalogue + suggest-shelters 0→6 candidats, L-001) ✅.
  - **Prochain :** Épic H — déploiement manuel (région SWA Canada + provisioning backend).

- **✅ Épic F — Accès invité (compte express silencieux) : MERGÉ vers `master` (2026-06-15) — PR #38 (`e536cd2`, merge `--admin`), branche `feat/epic-f-guest-checkout` supprimée.** Un visiteur non connecté peut acheter/louer/réserver ; à la confirmation il saisit nom/prénom/courriel/tél ; un **compte express passwordless** (`AppUser.IsExpress`, rôle `Customer`, migration `AddIsExpressToAppUser`, `MigrateOnStartup` reste OFF L-022) est **trouvé-ou-créé par courriel** → `CustomerId` réel partout, **aucun JWT émis** sur le parcours invité (login gate `CheckPasswordAsync` → false sur hash null). **Backend** : `GuestContact` + `GuestContactValidator` + port `IExpressAccountService` (Application, zéro fuite Identity) / impl `ExpressAccountService` via `UserManager` (Infrastructure, idempotence sur duplicate) ; les 3 commandes `PlaceOrder`/`CreateRentalContract`/`CreateBooking` reçoivent `GuestContact?` + `When(GuestContact is not null)` + résolution `userId = currentUser.UserId ?? express.FindOrCreate(...)` (sinon `BusinessRuleException`) ; `[AllowAnonymous]` sur les **3 POST de création seulement** (sœurs `GetMine`/`Cancel`/`Reschedule`/admin restent sous `[Authorize]` classe) ; `InternalsVisibleTo` ajouté à `Application.csproj` (handlers `internal sealed`, idiome Infrastructure). **Frontend** : composant réutilisable `app-guest-contact` (OnPush/signals/reactive ; `<label for>`, `aria-describedby` **conditionnel**, erreurs `role=alert` scopées, ≥44px, i18n `@@guestContact.*`) affiché si `!auth.isAuthenticated()` ; retrait `authGuard` de `panier/caisse` ; **retrait des gardes/redirections `/auth` impératives** dans `cart.checkout()`/location/installation (L-029) + redirection post-succès invité→`/`. **Sécurité tranchée** : courriel d'un compte réel existant → on **réutilise** ce compte (rattache la commande) sans ouvrir de session ; rôle figé `Customer`. **Cycle** : architect (plan F1→F6) → developer backend (F1+F2) → developer frontend (F3+F5) → **code-reviewer indépendant APPROVE WITH NITS** (zéro bloquant ; sécurité passwordless + sibling-actions vérifiée ; non-régression `PlaceOrderCommandValidatorTests` Ontario→pass intacte L-004 ; nit `aria-describedby` conditionnel appliqué sur le fil) → mentor (**L-028** revue sécurité bipartite `[AllowAnonymous]` ; **L-029** ouvrir une route ≠ juste retirer le `authGuard`). **Gates** : `dotnet test` **315/0** (32 nouveaux dont IT anonymes `[Collection("Integration")]` L-010) ✅ · `npm run build` ✅ · `npm test` **261/0** (guest-contact 5/5, 0 axe — *contraste non couvert vitest L-016*) ✅ · `npm run e2e` checkout-guest **2/2** Ontario+QC (`pressSequentially` + barrière réseau L-012) + non-régression 11 + a11y dual-thème 30 (0 axe) ✅ · i18n catalogues symétriques (10 ids ajoutés, 3 orphelins purgés des 2). **⚠️ CI — déploiement prod EN ATTENTE** : les 3 gates code (Backend / Frontend / SonarCloud) **verts**, mais `Build & Deploy` (SWA) échoue sur un **rate-limit MCR externe** (429 `mcrprod.azurecr.io`, incident Microsoft Container Registry, sans rapport avec le code) — merge `--admin` autorisé par l'utilisateur sur cette base. **Le push-master `Build & Deploy` (run `27580207119`) a aussi échoué sur le même 429 → la prod n'a PAS encore reçu Épic F. Re-run manuel quand MCR récupère : `gh run rerun 27580207119 --failed`.** **Prochain épic : G — catalogue marques/modèles + fit `/mesurer`.**

- **✅ Épic E — Parcours premier utilisateur : MERGÉ vers `master` (2026-06-15) — PR #37 (`233a388`), branche `feat/epic-e-first-user` supprimée, CI verte, prod auto-déployée.** Frontend pur. **E1** (redirection post-register → `/mon-compte/profil`) **était déjà en place** dans `auth.ts` — verrouillée par un test e2e de régression (L-005). **E2** : alerte non bloquante « entrez votre adresse » à la 1re connexion, **scopée page Profil** (`role=status`/`aria-live`, PAS de 3e région globale — L-010) ; marqueur **sans migration** = adresse profil vide (via `ProfileService`, L-003) + drapeau `localStorage` par userId (`FirstLoginHintService` SSR-safe). Reset neutre avant ré-annonce (L-027), focus post-rendu vers l'onglet Adresse (L-006), jetons sémantiques dual-thème (L-016), 3 trans-units dans les 2 catalogues (L-018). Cycle : architect → developer → **code-reviewer indépendant APPROVE WITH NITS** (zéro Critical/Major ; nit assertion userId vide appliqué ; mentor : aucune nouvelle leçon, cas exemplaire). Gates : `npm run build` ✅ · `npm run i18n:extract` (666 msgs, catalogues symétriques) ✅ · `npm test` **256** ✅ · `npm run e2e` (first-login-hint 5/5 + non-régression, 0 axe) ✅. **Bug hors périmètre signalé** : onglet actif `.profile-tab.is-active` blanc-sur-rouge primaire ~2.76:1 en thème sombre (préexistant, non introduit ici — loggé pour cycle dédié). **Prochain épic : F — accès invité (compte express silencieux).**

- **✅ Épic D — Système d'adresse unifié : MERGÉ vers `master` (2026-06-15) — PR #34 (`9c91254`), branche `feat/epic-d-address-unified` supprimée, CI verte (Backend / Frontend+i18n+axe 6m38s / Build & Deploy / SonarCloud), prod auto-déployée.** *Gate Sonar à 2 itérations : duplication new-code 5.5 %→3.8 %→<1 % (câblage adresse extrait dans `AddressFormController` **auto-injecté** — `inject()` interne valide en initialiseur de champ, écrans réduits à `addr = createAddressFormController(this.form)`) + bug fiabilité `Web:InputWithoutLabelCheck` corrigé (`aria-label` i18n sur l'input fichier avatar `profile.html`).* 3 commits : `240e46a` **D1–D3** (autofill robuste : n° civique préservé via cascade `s.civicNumber || parseCivicFromLabel(label) || valeur saisie` ; code postal → `PostalFillResult` `filled`/`unavailable` + `aria-live`, court-circuit sans réseau si la suggestion porte déjà le code ; province/ville verrouillées par tests, **aucune** whitelist L-004 §C1) ; `6354645` **D4–D5** (carte `/mesurer` centrée sur l'adresse géocodée — `PlacesService.geocode` réutilise `suggest`, `invalidateSize` au montage `@defer`, base Gatineau promue en `SERVICE_BASE` ; zone de service 100 km = avertissement **doux non bloquant** `role="status"`, util client `service-area.util` + Domain `GeoDistance` **miroir** L-007, **sans** `RuleFor` distance + test serveur de non-rejet) ; `997feaf` **D6** (composant partagé `app-address-choice` : pastille profil `role="group"` lecture seule **ou** bascule `<ng-content>` vers le parcours anonyme, câblé sur les 4 écrans caisse/location/installation/mesurer ; focus post-rendu **L-006** + `aria-live` scopée **L-010** ; `applyDefaultAddress(form, address?, force?)` recopie l'adresse profil pour un payload valide en mode pastille ; parcours anonyme **strictement inchangé** quand `profileAddress` null). Cycle : architect (×3 unités) → developer (1 à la fois) → **code-reviewer indépendant à la frontière d'épic : APPROVE WITH NITS** (zéro Critical/Major ; miroir client↔serveur identique vérifié ; warning non bloquant confirmé ; SOLID `GeoDistance` OK) → mentor (**L-026**, **L-027** ajoutées ; **L-016** affûtée). Gates : `npm run build` ✅ · `npm test` **244** ✅ · `npm run i18n:extract` (9 ids symétriques, L-018) ✅ · `npm run e2e` (address-choice 10 + régression dual-thème, 0 axe) ✅ · `dotnet test` **211+72/0** ✅. **Bug-09 découvert (préexistant, hors périmètre)** : badge « Ajusté serré » `/mesurer` résultats illisible en sombre (1.66:1), loggé `board.md`, cycle dédié. *Reste : push branche → PR → CI verte → merge `master` (prod auto-déployée).*

- **✅ Épic C — Bascule de langue (EN) en local : MERGÉ vers `master` (2026-06-14) — PR #33 (`a4ad2d2`), branche `feat/epic-c-locale-dev` supprimée, CI verte (Backend 57s / Frontend+i18n+axe 5m56s / Build & Deploy / SonarCloud), prod auto-déployée.** Constat clé : **l'essentiel existait déjà sur `master`** (`LocaleService` complet, bouton langue navbar unique, i18n `navbar.langTo*`/`locale.switched` synchronisés, specs `language-switch*.spec.ts` + `locale.service.spec.ts`, `scripts/serve-i18n.mjs`, ces specs déjà en CI). Le diff se limite aux **3 manques réels** : (1) **script `dev:i18n`** (`= build:i18n + serve-i18n.mjs`, fr `/` + en `/en/`) — UNE commande pour exercer la bascule en local ; (2) **détection mono-locale vs bi-locale via flag `environment.localized`** (false en dev, true en prod/staging — import statique **SSR-safe**, pas de `window`) → `LocaleService.localized` (computed) + `switchTo()` **no-op** quand `!localized()` ; (3) **dégradation accessible du bouton « EN »** sous `ng serve` : reste un `<button>` **focusable** avec `[attr.aria-disabled]`/`[attr.aria-describedby]` → `<span class="sr-only">` **scopé navbar** (PAS de `role="status"` global — L-010) + `title` `$localize` (`@@navbar.langUnavailable`, **2 catalogues** L-018), **pas** de `disabled` natif. **Major attrapé en revue + corrigé** : config `staging` d'`angular.json` n'avait pas `"localize": true` alors que `environment.staging.ts` posait `localized: true` → un build staging aurait servi mono-locale FR en se prétendant bilingue (redirection silencieuse, la régression même qu'on corrige) ; fix = `"localize": true` ajouté à la config staging (miroir de `production`), **build staging vérifié vert** (fr+en). **Leçons L-024** (dégradation `aria-disabled` focusable + `title` `$localize`, pas `i18n-` sur attribut lié) **et L-025** (un flag `environment.*` adossé à une option de build doit s'accorder avec la config `angular.json` de **chaque** configuration — les configs sibling ne fusionnent pas) capturées. Cycle : architect → developer → **code-reviewer indépendant REQUEST CHANGES → corrigé → APPROVE** → mentor. Gates : `npm run i18n:extract` (650 msgs, 0 warn, catalogues symétriques) ✅ · `npm run build` ✅ · `npm test` **206** (0 axe — *contraste non couvert vitest L-016*) ✅ · `npm run e2e` **79** (chromium mono-locale : bouton EN dégradé sans redirection ; projet i18n :4300 bilingue inchangé = non-régression prod) ✅ · `ng build --configuration staging` ✅ (Major) · **CI verte + merge `master`** ✅. *Note hors-périmètre relevée : `.navbar__lang-btn` < 44px (pré-existant, non touché) — à traiter dans une passe a11y dédiée.*

- **✅ Épic B — Curseur custom (point + anneau magnétique) : MERGÉ vers `master` (2026-06-14) — PR #32 (`56e7409`), branche `feat/cursor-ring-magnetic` supprimée, CI verte (Backend / Frontend+axe+e2e 5m45s / Build & Deploy / SonarCloud), prod auto-déployée.** Refonte de `shared/components/cursor-ring/` en **deux éléments** : un **point** net (8px) qui suit le pointeur instantanément (`translate3d`, transition opacity seulement) + un **anneau** fin (36px) en léger retard via boucle **rAF lerp auto-arrêtée** (FACTOR 0.18, stop sur EPSILON → zéro frame au repos), qui **grossit (56px) + prend une teinte douce** (`--color-primary-subtle`) au survol d'un interactif (`a, button, [role=button]`) via la classe `cursor-ring__ring--active`. Magnétique **délégué sur `document`** (`pointerover`/`pointerout` + `closest`), anti-spam (`classList.toggle` seulement sur transition d'état). **Gardes conservées et étendues aux DEUX éléments** : host `aria-hidden`, `pointer-events:none`, **jamais `cursor:none`**, actif seulement `pointer:fine` ET `!prefersReducedMotion()`, init `afterNextRender` (SSR-safe), repli CSS `@media (pointer:coarse),(prefers-reduced-motion:reduce){display:none}`. **Teardown complet** au `DestroyRef` (4 listeners + `cancelAnimationFrame`). Cycle : architect → developer → **code-reviewer indépendant APPROVE WITH NITS** (zéro Critical/Major ; nit `isActive` relâché en `onLeave` appliqué ; aucune leçon nouvelle — L-006/L-009/L-016 déjà bien appliquées). Gates : `npm run build` ✅ · `npm test` **203** (cursor-ring 3/3, 0 axe — *contraste NON couvert en vitest L-016, anneau décoratif `aria-hidden`*) · `npm run e2e` **motion-a11y 8/8** (Bloc A reduced-motion réel → dot+ring cachés ; Bloc B axe dual-thème navbar scrollée). *Suggéré (non bloquant) : round-trip live L-001 du survol magnétique à l'œil dans les deux thèmes — l'effet décoratif n'est vérifiable qu'en visuel.*

---

## Curseur courant (Programme A→F — TERMINÉ, conservé pour traçabilité)

- **🎉 PROGRAMME TERMINÉ (Épics A→F tous mergés vers `master`).** **Épic F mergé — PR #19 (`62e6981`)**, branche `docs/program-wrapup` supprimée, CI verte (Backend / Frontend / Build & Deploy / Sonar). *(1 flake CI confirmé : `webgl.util.spec` — mock pur déterministe — a échoué une fois sur un hoquet vite « dependency scan failed » ; re-run vert, L-017.)* Revue indépendante APPROVE WITH NITS (nits clôturés `5adb1f3`), L-018 capturée. Il ne reste **aucune sous-tâche de programme**.
- **✅ F2-B + F2-C livrés et MERGÉS vers `master` (2026-06-14).** **PR #21 mergée (`5665677`)**, branche `fix/f2-heuristics-followup` supprimée, **CI verte** (Backend / Frontend+axe / Build & Deploy / SonarCloud). Revue indépendante **APPROVE WITH NITS** (nits clôturés), gates verts (`npm test` 202, `build:prod` 499,71 kB, e2e contraste dual-thème du bouton danger OK clair+sombre). Leçons **L-019**/**L-020** capturées.
- **✅ F2-D livré et MERGÉ vers `master` (2026-06-14) — PR #23 (`a57ed5e`)**, branche `fix/f2d-mesurer-geoman-draw` supprimée, **CI verte** (Backend / Frontend+axe+e2e 5m32s / Build & Deploy / SonarCloud). `/mesurer` mode carte **désormais dessinable**. Cause réelle (≠ hypothèse CJS/ESM du pointeur) : geoman est un **IIFE qui lit `L` en variable libre via `globalThis.L`** (n'importe pas Leaflet) **et** attache `map.pm` par un `L.Map.addInitHook` ne s'exécutant que pour les cartes construites **après** son chargement. Fix couplé (`map-measure.ts`, `afterNextRender`, SSR-safe) : `globalThis.L = L` **puis** import geoman **avant** `L.map(...)`. Garde e2e `test.fixme`→`test` actif (capacité `.leaflet-pm-toolbar.leaflet-pm-draw`). Revue indépendante **APPROVE WITH NITS** (Major commentaire périmé L-008 + nits clôturés). Gates : `npm run build` ✅ · `npm test` **202** (0 axe) ✅ · `build:prod` (geoman lazy 359,97 kB) ✅ · `npm run e2e` **73 passées / 0 échec** (ex-`fixme` vert). **Leçon L-021** capturée ; **L-019** corrigée (diagnostic CJS/ESM supersédé). *Vérif live L-001 faite côté dev (vite, sonde avant/après) ; fix runtime/bundler-agnostique → attendu identique en prod.*
- **✅ Flake-01 livré et MERGÉ vers `master` (2026-06-14) — PR #25 (`288df31`)**, branche `fix/flake-01-catalog-loading-status` supprimée, **CI verte** (Backend / Frontend+axe+e2e 5m25s / Build & Deploy / SonarCloud). La grille squelette de chargement de la Boutique (`catalog.html`) portait `aria-busy` + `aria-label` sur un `<div>` **sans `role`** → axe levait `aria-prohibited-attr` par intermittence (fenêtre de chargement). Fix : **`role="status"`** sur la grille (live-region qui autorise `aria-label`/`aria-busy` **et** préserve l'annonce « Chargement des produits… » — les cartes squelette sont `aria-hidden`, donc retirer le label aurait rendu la région muette). **Garde de test non vacueuse** (`catalog.component.spec.ts`, `getProducts: () => NEVER` maintient `loading()` vrai → scanne axe sur l'état de chargement ; échouerait sans le `role` — `aria-prohibited-attr` actif en vitest, seul `color-contrast` y est désactivé, L-016). Pas de collision `getByRole('status')` (L-010 : tous les locators e2e filtrés par texte ; les deux `status` du template sont en branches `@if`/`@else` mutuellement exclusives). Revue indépendante **APPROVE**. Gates : `npm run build` ✅ · `npm test` **203** (0 axe) ✅ · `npm run e2e` Boutique **8/8** (clair+sombre, navbar scrollée) ✅. *(1 flake CI confirmé au 1er run : `webgl.util.spec` — hoquet vite « dependency scan failed », sans rapport avec le diff ; re-run vert, L-017.)*
- **✅ Déploiement prod : AUTOMATIQUE et À JOUR (vérifié 2026-06-14, L-001).** Le secret `AZURE_STATIC_WEB_APPS_API_TOKEN` est **en place depuis 2026-06-12** → **chaque merge vers `master` déploie en prod tout seul** via `.github/workflows/azure-static-web-apps.yml` (Azure Static Web Apps, plan Free). **Aucune action manuelle.** Le run push-master du merge de PR #26 a publié avec succès (log : « Deployment Complete :) · Visit your site at: https://kind-water-0efca2b10.7.azurestaticapps.net ») ; `/` (fr) et `/en/boutique` (en, deep-link) répondent **200**. Toutes les réparations (hero statique, bouton unique, contraste, F2-B/C/D, **grille de chargement Boutique Flake-01**) sont **déjà en ligne**. *(Correction d'un pointeur périmé : l'ancienne note « redéploiement requis, gated sur un secret » était fausse — le secret existait déjà et le CD est automatique.)*
- **✅ Flake CI `webgl.util` éliminé à la racine — MERGÉ (PR #28, `da4bf4f`).** Le test était 100 % pur/déterministe ; il tombait quand le scanner de deps de Vite crawlait `dist/**/index.server.html` (sortie `build:prod` lancée avant `npm test` en CI) et échouait sur les bundles serveur hachés (« Failed to run dependency scan »). Fix : `optimizeDeps.entries = ['src/**/*.spec.ts']` dans `vitest.config.ts` → le scan ignore `dist/`. Vérifié : `build:prod` puis `npm test` → 203 passés, plus aucun message de scan. Revue **APPROVE**. *(Ce flake n'apparaît plus depuis ; les re-runs à répétition sont finis.)*
- **🟡 Déploiement BACKEND : PRÉPARÉ (non provisionné) — MERGÉ (PR #29, `f5939d3`).** Sur choix utilisateur : **Azure Container Apps** (API .NET 10, scale-to-zero) + **Azure SQL Database S0** (gratuit 12 mois, provider EF inchangé), front restant sur SWA. Artefacts livrés et inertes (workflow gated sur `AZURE_CREDENTIALS`, comme le SWA) : `Dockerfile` multi-étapes (non-root, 8080) + `.dockerignore` ; `.github/workflows/azure-container-app.yml` (build ACR Tasks + `az containerapp update`) ; **migration EF opt-in au démarrage** (`Database__MigrateOnStartup`, OFF par défaut → dev/tests intacts) ; CORS prod repointé sur l'URL SWA ; `environment.prod.ts apiUrl` en placeholder à substituer post-provisioning ; **`docs/deployment.md` §4.2** = séquence `az` complète à dérouler. Revue indépendante **code-reviewer + solid-review : APPROVE WITH NITS** (nits clôturés). **Leçon L-022** capturée (migration opt-in OFF par défaut ; `ASPNETCORE_FORWARDEDHEADERS_ENABLED` contre la boucle 307). Gates : `dotnet test` 274 · `dotnet publish` Release · `npm run build` · CI verte (2 Security Hotspots Sonar — image root + `azure/login` non épinglé — **corrigés en code**, pas contournés). **À FAIRE par l'utilisateur quand il voudra déployer** : dérouler `docs/deployment.md` §4.2 (`az login` → ressources → `gh secret set AZURE_CREDENTIALS` → substituer le FQDN dans `environment.prod.ts` → push `master`).
- **Reste uniquement le provisioning Azure réel du backend** (manuel, gated sur la connexion `az` de l'utilisateur ; tout est scripté). Côté frontend + programme A→F : entièrement livré ET déployé.

---

### Historique (Épic F, clos — conservé pour traçabilité)

- **Épic clos :** **Épic F — Wrap-up docs/process** (branche `docs/program-wrapup` — créée depuis `master` post-merge E, active). Voir le plan (Épic F) : (1) ✅ `.claude/rules/motion-a11y.md` + hook PostToolUse étendu ; (2) ✅ passe heuristique fraîche → table F2 dans `docs/ux/heuristic-evaluation.md` ; (3) README : état final de la roadmap + ajouts « Compétences démontrées » (three.js, proxy géospatial, migration EF owned-entity — **NB : « GSAP scroll-craft » retiré du showcase**, voir l'écart hors-programme ci-dessous) ; (4) CI sanity : confirmer que `ci.yml` exécute tous les nouveaux specs + budgets prod.
- **Prochaine étape :** **🏁 Clôture d'épic F — PR `docs/program-wrapup` → `master`** (autorisée par l'utilisateur : « corriger F2-A d'abord, puis revue → PR → master »). F1→F4 + F2-A + nettoyages de revue tous faits ; revue indépendante **APPROVE WITH NITS → nits clôturés** ; L-018 capturée. Reste : pousser la branche, ouvrir la PR, **CI verte**, merge vers `master`. **F2-B/F2-C** (mineurs heuristiques) restent ouverts comme suivi post-merge.
- **Revue indépendante (code-reviewer) : ✅ APPROVE WITH NITS** sur le diff `master..HEAD` — zéro Critical/Major. Q vérifiées OK : deep-link auth (init `view` depuis `route.snapshot` — `route` déclaré avant `view`, SSR-safe, pas de flip-flash) ; hero (aucun contenu a11y/i18n perdu, SSR strictement meilleur) ; focus admin/products (miroir fidèle de bookings, tests non vacueux) ; CI `build:prod` (budgets justifiés, marge 0,51 kB serrée mais voulue). **2 Minors + nits clôturés** (commit `5adb1f3`) : `gsap` retiré (package.json+lockfile) ; trans-units i18n orphelins purgés des DEUX catalogues ; commentaire `reveal-on-scroll` corrigé ; **e2e `auth-register-deeplink.spec.ts`** ajouté (garde CI du deep-link, L-005). **Leçon L-018** (mentor) : finir un retrait à TOUS les niveaux (dep + lockfile ; trans-units des 2 catalogues).
- **F4 — CI sanity : ✅ FAIT** — specs **auto-globés** confirmés (vitest `npm test` + Playwright `npm run e2e` découvrent tout nouveau `*.spec.ts`/`e2e/*.spec.ts` — `motion-a11y`, `shelter-3d` inclus). **Gap trouvé + corrigé** : `ci.yml` buildait en `--configuration development` (`npm run build`) → **budgets non appliqués** (les budgets vivent sur la config `production` : initial 500 kB warn/1 MB error, anyComponentStyle 10/16 kB). Étape CI passée à **`npm run build:prod`** (budgets + bundle localisé /en/ + postbuild i18n). **Prouvé vert localement** : `build:prod` exit 0, **initial 499,49 kB** (transfer 143,02 kB) — sous le seuil, le garde-fou est désormais réel et serré (L-005).
- **F3 — README état final : ✅ FAIT** — feuille de route : ligne **Redesign v2** ajoutée (jetons sémantiques, viewer 3D three.js `@defer`, micro-interactions reduced-motion, contraste axe dual-thème, hero statique). « Compétences démontrées » : ajout **Visualisation 3D** (three.js descripteur pur testé sans WebGL, lazy, clavier), **Géospatial** (proxy Photon/Radar/Google + Leaflet/turf), **Données & migrations** (EF owned-type `Address` éclaté avec préservation des données). **« GSAP scroll-craft » NON ajouté** (hero GSAP retiré).
- **F2 — passe heuristique fraîche : ✅ FAIT (cette session, commit local à suivre)** — nouvelle section « Passe heuristique fraîche (2026-06-14) — Épic F / F2 » dans `docs/ux/heuristic-evaluation.md`, 3 constats vérifiés contre le code + surfaces saines confirmées. **F2-A (majeur, 3)** : `admin/products` ouvre son `alertdialog` de suppression **sans gestion du focus** (focus non déplacé/non renvoyé ; `keydown.escape` inopérant) — **incohérent** avec `admin/bookings`+`admin/rentals` qui le gèrent (pattern L-006). **F2-B (mineur, 2)** : `/mesurer` adresse tapée à la main (lat/lng null) → `map-measure` retombe sur Gatineau **sans avertir**. **F2-C (1–2)** : `.btn--small`/`.btn--danger` dupliquées ×3 + divergence du global `.btn--sm`. **🔧 Candidat remédiation hors-wrapup : F2-A** (réutiliser le pattern focus de bookings/rentals + test clavier) — à planifier si l'utilisateur veut clore la barre a11y dure sur l'admin.
- **🔧 Écart hors-programme réconcilié (commit `cf0bf64`, session 2026-06-14)** — sur demande utilisateur, **retrait complet du hero « scroll story » GSAP** (E2) : le hero d'accueil est désormais une **section statique** qui défile normalement (plus d'épinglage/scrub, plus de SVG construit). `gsap`+`ScrollTrigger` **disparus du bundle navigateur** (~275 kB JS en moins — gain perf direct sur le score Lighthouse 0,49). Mêmes commits : **fusion** du duo navbar Connexion/S'inscrire en **un seul** bouton « Connexion » (desktop + mobile) ; **`/auth?vue=inscription`** ouvre directement l'inscription (corrige « S'inscrire → connexion »). `motion-a11y.spec.ts` Bloc A adapté (assertions `data-motion`/pin-spacer GSAP retirées), `hero-story.spec.ts` réécrit. **Conséquences pour Épic F** : (a) F1 `motion-a11y.md` documente les reveals **IntersectionObserver** (`reveal-on-scroll`), pas l'épinglage GSAP ; (b) F3 README — retirer « GSAP scroll-craft » des compétences démontrées. Gates de la réparation : `build` ✅ · vitest **196/196** · e2e `motion-a11y` **8/8** (contraste dual-thème vert — l'illisibilité signalée venait du **prod périmé**, pas du code). *(Non mergé ; vit sur `docs/program-wrapup` avec le travail Épic F.)*
- **F1 — motion/a11y rule + hook : ✅ FAIT (cette session, non committé au moment d'écrire → commit local à suivre)** — `.claude/rules/motion-a11y.md` : checklist 5 volets (reduced-motion ceinture+bretelles ; jetons-sémantiques-seulement + contraste vérifié en e2e pas vitest L-016 ; cartes/3D/autocomplete `@defer`+import dynamique SSR-safe + proxy places ; widgets composites APG L-015 ; hygiène e2e/locators L-005/008/010/017). Hook `post-edit-guardrail.mjs` étendu : nouveau prédicat `isMotionOrTokens` (tout `.scss` + fichiers motion/maps nommés) → pousse vers la checklist. Hook **testé** (scss→frontend+motion ; .cs→backend seul ; .ts non-motion→frontend seul ; JSON valide).
- **Épic E — ✅ TERMINÉ ET MERGÉ vers `master`** — **PR #18 mergée (`09f2d37`)**, branche `feat/redesign-v2` supprimée, CI verte (Backend / Frontend / Build & Deploy / Sonar). Revue indépendante `code-reviewer` E1→E5 : APPROVE WITH NITS. Détail E1→E5 ci-dessous. **Reste recommandé (non bloquant, post-merge) :** round-trip live L-001 à l'œil (reduced-motion ON/OFF, 2 thèmes, glass-on-scroll, overlay, viewer 3D + repli) + relevé Lighthouse manuel.
- **E5 — Perf gates + remédiation contraste : ✅ FAIT (clôture Épic E ; commit local, non mergé)** — **deux** régressions de contraste du redesign corrigées au niveau jeton (pas en patch local) : (1) navbar `.navbar__brand-text strong` (« Tempo ») + `.navbar__brand-icon` repointés de `--color-primary-light` (#dc2626, **2.11:1** en clair) → **nouveau jeton FIXE** `--color-brand-on-dark: #f87171` (couche sur-sombre, **non surchargé en dark**) → **≈3.75:1** composé sur `.navbar--scrolled` / **6.41:1** navy plein, **passe 3:1 dans les DEUX thèmes** (gras 20px = grand-texte) ; (2) **découverte au scan dual-thème** (pas au constat ponctuel) : bouton « Voir en 3D » `.detail__view3d` réutilisait `.btn--secondary` (texte blanc fixe conçu pour surface sombre) sur la fiche claire → **1.1:1** ; recoloré via jetons qui basculent par thème (`--color-text`/`--color-bg-muted`). Nouveau **`e2e/motion-a11y.spec.ts`** (8 cas) : bloc A reduced-motion **réel** (`emulateMedia` — hero figé `data-motion=reduced` + `.pin-spacer` count 0, cursor-ring dot opacity 0, viewer 3D stable) + bloc B **axe dual-thème navbar scrollée** (`color-contrast` inclus — verrouille le volet contraste, comble le trou glass que `a11y.spec.ts` n'exerçait pas). `e2e/reschedule.spec.ts` : scan **pleine page** (exclusion `app-reservations` retirée — **L-008**). Bundle initial figé confirmé : `index.html` sans `<script>` gsap/three/leaflet. **L-017** appliqué (architecte a tué un `ng serve` zombie PID 2808 sur 4300 ; `netstat` 4200/4300 vide avant chaque e2e). Gates : `build` ✅ · `npm test` **196** (axe vitest=0 — *qualifié : `color-contrast` NON couvert ici, L-016 ; contraste validé en e2e*) · `build:prod` ✅ · **`npm run e2e` 71** (motion-a11y 8/8, admin-management 3 thèmes-clair repassent, reschedule pleine page, shelter-3d) ✅. **Revue indépendante `code-reviewer` : APPROVE WITH NITS** (zéro Critical/Major ; 2 nits optionnels ; ratios contraste **recalculés indépendamment** par le reviewer ; famille « jeton on-dark sur surface claire » grep → aucune autre fuite). Gate Lighthouse **manuel** (LCP≤2,5s/CLS≤0,1/INP≤200ms) documenté non-bloquant dans `wcag-2.2-audit.md` §5.9.
- **E4 — Viewer 3D : ✅ FAIT (commit local `02c69d2`, non mergé)** — dép `three`+`@types/three` (ESM, pas d'`allowedCommonJsDependencies`). `shared/components/shelter-3d-viewer/` : `shelter-model.builder.ts` = `buildShelterDescriptor` (**PUR, zéro import three, testé sans WebGL**) + `descriptorToGroup(desc, THREE)` (namespace injecté) ; arcs tube demi-ellipse + pannes + toile courbée depuis `widthCm/lengthCm/heightCm` (D1). `webgl.util.isWebglAvailable` SSR-safe. Composant : import dynamique three+OrbitControls dans `afterNextRender` (garde `isPlatformBrowser`+WebGL) ; canvas `role=img`+label ; **boutons clavier** rotation/zoom/reset ≥44px `aria-label` ; pas d'auto-rotate sous reduced-motion (`MotionService`) ; rendu à la demande ; teardown WebGL (`dispose`+`forceContextLoss`) au `DestroyRef` ; repli `<img>` si no-WebGL/SSR. `product-detail` : `@if(has3dDims) { @defer(on interaction) }` placeholder « Voir en 3D »/`@loading`/`@error` repli image — monté **seulement si les 3 dims existent**. i18n 8 chaînes fr/en. `e2e/shelter-3d.spec.ts` (dans CI) : defer via locator (barrière post-condition L-012), clavier, `emulateMedia` reduced-motion, axe-clean. **`three` 100 % LAZY** (4 chunks WebGL absents d'`index.html`) ; **bundle initial 143,04 kB transfer INCHANGÉ**. Gates : `build` ✅ · `build:prod` ✅ (budget initial OK) · `npm test` **196** (zéro axe vitest) ✅ · `e2e shelter-3d` 3/3 ✅. **L-010 vérifié** (aucun `getByRole('img'|'status'|'alert')` non scopé). *(Échecs e2e contraste ci-dessus = volet (a), préexistants à E4, isolés via `git stash`.)*
- **E3 — Micro-interactions : ✅ FAIT (commit local `1e38a4d`, non mergé)** — `MotionService` SSR-safe (`signal prefersReducedMotion`, `matchMedia` + `change`) ; 3 directives `shared/directives/` : `reveal-on-scroll` (IntersectionObserver, état masqué posé **en JS pas en CSS** → pas de CLS/contenu invisible si SSR), `count-up` (rAF, **décoratif sans `aria-live`**, valeur finale immédiate sous reduced-motion/SSR — assertion non vacue L-002), `magnetic-hover` (`pointer:fine`, transform borné, reset `pointerleave`) ; `loading-overlay` câblé `Router.events` (`role=status aria-live polite`, SVG arche `aria-hidden`, non-modale, figé sous reduced-motion) ; `cursor-ring` (`aria-hidden`, `pointer-events:none`, **jamais `cursor:none`**, `pointer:fine` + reduced-motion off) ; navbar glass-on-scroll (**SCSS seul**, signal `scrolled()` déjà existant). i18n `@@loadingOverlay.label` (fr/en). **L-010 vérifié** : balayage suite e2e+specs → aucun `getByRole('status'|'alert')` non scopé (tous déjà filtrés par texte / rendus isolés). Gates : `npm run build` ✅ · `npm test` **180** (zéro axe) ✅ · `e2e` 58 ✅ (2 échecs `/en/` **préexistants/environnementaux** — serveur prod-i18n port 4300 ; `build:prod` vert prouve que le bundle `/en/` compile, donc non lié à E3). **À FAIRE à la frontière d'épic E** : revue indépendante `code-reviewer` (couvre E1→E5) + round-trip live L-001 (reduced-motion ON/OFF réel, thème sombre, glass-on-scroll, overlay à la navigation, cursor-ring pointeur-fin seulement) + scénario Playwright `motion-a11y` (émulation `prefers-reduced-motion` réelle — déféré E5 vu que vitest-browser n'émule pas les media-queries).
- **E2 — Hero scroll story : ✅ FAIT (commit local `651afa0`, non mergé)** — `app-hero-story` (GSAP ScrollTrigger pin+scrub, SVG 4 couches, beats) remplace le hero inline. SSR-safe (import dynamique `afterNextRender`, interop CJS/ESM `?? .default`, frame 1 serveur = scène finale, pin-spacer 100dvh → pas de CLS au chargement). reduced-motion → scène figée (JS + repli CSS). i18n : 13 `@@home.hero.*` déplacés verbatim (zéro churn) + 4 `@@home.heroStory.beat*`. `gsap` ^3.13.0 en chunk lazy (~16 kB transfer, bundle initial inchangé). Gates : `npm test` **165** (axe=0, 2 thèmes) ✅ · `build:prod` ✅ (gsap lazy, budgets OK) · `e2e` ✅. **À FAIRE à la frontière d'épic** : (1) round-trip live L-001 (reduced-motion ON/OFF + **vérif CLS Lighthouse à l'œil** + thème sombre) ; (2) revue indépendante `code-reviewer`.
- **E1 — Tokens v2 : ✅ FAIT (commit local `cdd82a4`, non mergé)** — bloc additif « Tokens v2 — Épic E / E1 » dans `_tokens.scss` (échelles navy/rouge, neutres chauds, couche sémantique sur-sombre/sur-marque, élévation/dégradés/lueurs, tokens mouvement) + balayage `styles.scss`/`home.scss`/`navbar.scss`. Bloc Épic A conservé. Gates : `npm test` 161 (zéro axe) ✅ · `build:prod` ✅ · `e2e` ✅.
- **🔖 Candidat leçon (à confier au `mentor` à la frontière d'épic)** : *vitest-browser n'a PAS d'émulation de media-query* (`page.context().emulateMedia` / `emulateMedia` inexistants sur `@vitest/browser` ; appel → `page.context is not a function`, l'erreur dans un `afterEach` casse le reset TestBed → « test module already instantiated » en cascade). Règle : en spec vitest, asserter le contrat mode-indépendant (contenu présent/atteignable + axe) + le hook reflété (`data-motion`) ; déférer l'émulation `prefers-reduced-motion`/thème à Playwright (E5).
- **Épic D : ✅ TERMINÉ ET MERGÉ vers `master`** — **PR #17 mergée (`fa8852b`)**, branche `feat/mesurer-parking` supprimée, CI verte (Frontend/Backend/Build&Deploy/Sonar). Détail ci-dessous.

### Épic D — ✅ TERMINÉ (revu, branche `feat/mesurer-parking`)

| Sous-tâche | État | Commits |
|-----------|------|---------|
| D1 — Dimensions produit (`WidthCm/LengthCm/HeightCm` + migration `AddProductDimensions` + seeder + projections + Create/Update validators + constante `ProductDimensions`) | ✅ | `a526d90` |
| D2 — Endpoint `suggest-shelters` (filtre dims ≥ requis, tri empreinte, marges + `IsTightFit`, validator >0∧≤2000) | ✅ | `b3408f0` |
| D3 — Feature `/mesurer` (stepper Adresse→Mesure→Résultats ; calculateur clavier ou carte Leaflet/geoman/turf `@defer` SSR-safe ; **cm-canonique + affichage en pieds** `units.util` ; radiogroups **APG** `radio-nav.util`) | ✅ | `01983b4` |
| Correctifs revue indépendante (radiogroups APG roving tabindex+flèches, wording, sealed, modifier-keys) | ✅ | `53d99fd`, `88fa70a`, `bd8381d` |

**Revue indépendante** : `code-reviewer` **REQUEST CHANGES → corrigée** (1 Major a11y : `role=radio` sans contrat APG, invisible pour AXE) ; `solid-review` backend **APPROVE WITH NITS** (validateurs scellés). **Leçons capturées : L-015** (APG roving tabindex ; focus synchrone post-`set` sûr si élément monté — contre-partie L-006) **et L-014** (`fb.control<number|null>`, jamais un tuple spread).
**Gates finales** : `npm test` **161** (zéro axe) ✅ · `npm run build:prod` (fr+en, i18n OK, chunk `/mesurer` ~28 kB gz) ✅ · `npm run e2e` `/mesurer` (clavier + smoke carte) + sweep dual-theme ✅ · `dotnet test` **274** ✅. *(Flake connu : 1er run e2e à froid → `vite-error-overlay` transitoire du dev-server ; vert au re-run warm + retries CI.)*
**Décision produit pliée** : **cm-canonique + affichage en pieds** — tout le calcul/API en cm ; les pieds ne servent qu'à la saisie (`feetToCm`) et à l'affichage (`cmToFeet`, « pi »/« ft », 1 décimale). Saisie manuelle en pieds (1–65 pi).
**Docs retournées** : `board.md` (clôture Épic D), `product-backlog.md` (US-1.7), README roadmap, `wcag-2.2-audit.md` (4.1.2 radiogroup APG).
**Statut git** : **PR #17 mergée vers `master` ✅ (`fa8852b`)**, branche supprimée. CI verte (4 correctifs e2e/CI après la 1re revue : APG, `@defer on immediate`, carte avant geoman, **interop CJS/ESM Leaflet `L.map`**).

- **Dernière mise à jour :** 2026-06-18 (**EPIC 9.3 — boutique/product-detail + configurateur de dimensions (front) FAIT, 5 commits locaux `aa48d7c`→`cd73d62`, branche `feat/epic-9-dimension-catalog`.** Front-only consommant les 3 endpoints `/api/v1/shelters` de 9.2. Livré : `core/models/shelter.model.ts` (3 interfaces miroir DTO + helpers catégories paramétriques) ; `core/services/shelter.service.ts` (`getModels/getModel/getPrice`, calque `ShelterSuggestionService`) ; `features/mesurer/util/feet-inches.util.ts` (`formatFeetInches` réutilisant `cmToFeet`, `units.util` NON touché — « 11 pi » / « 6 pi 6 po ») ; **composant `dimension-configurator`** (largeur+hauteur = radiogroups APG via `radio-nav.util` L-015 ; longueur = range+number reactive ; recalcul prix debounce 300ms via `/price` + miroir optimiste de `ShelterPriceCalculator` ; `aria-live` prix avec réannonce neutre L-027 ; `output()` config pour 9.4) ; pages `shelter-models` (liste par catégorie) + `configure-shelter` (hôte) ; routes lazy `modeles/:category` & `configurer/:slug` AVANT le catch-all `:slug` ; CTA « Configurer » dans `catalog` + lien dans `product-detail` pour catégories paramétriques ; i18n symétrique 2 catalogues (L-018). **Contraste : une vraie violation light-theme attrapée par l'e2e dual-thème** (`--color-text-muted` #6b7280 sur `--color-bg-muted` = 4.39:1, **re-frappe du piège L-016/L-033** sur surface teintée fraîche) corrigée au jeton (`--color-text`) en 3 endroits. **Gates** : `npm run build` ✅ · `npm run i18n:extract` 723 msgs 0 warn (ids symétriques) ✅ · `npm run build:i18n` (bilingue) ✅ · `npm test` **276/276** (14 nouveaux ; *axe vitest — color-contrast NON couvert L-016*) ✅ · `npm run e2e` **118** (4 nouveaux + boutique/product-detail axe dual-thème, 0 violation après fix contraste) ✅. **À confier au `mentor` à la frontière d'épic** : (a) re-frappe L-016 sur texte muet/fond teinté (couvert par L-016/L-033, valider que la formulation suffit) ; (b) **15 ids orphelins préexistants dans `messages.en.xlf`** (`home.catalog.*`, `cart.orderError`… sans source FR — dérive antérieure à 9.3, nettoyage L-018 latent, hors périmètre). **Revue indépendante `code-reviewer` + `solid-review` DIFFÉRÉE à la frontière EPIC 9** (après 9.5), conformément à l'économie de crédits (sous-tâche intermédiaire = commit local). **Curseur → EPIC 9.4** (panier/parcours/commande : la dimension configurée suit l'article + recalcul prix serveur dans `PlaceOrder`). Découpage restant : 9.4 panier/commande → 9.5 admin référentiel.) *Historique :* (**EPIC 9.2 — API catalogue d'abris paramétrique + calcul de prix FAIT, commit local `8bd109b`.** 3 queries CQRS (GetShelterModels, GetShelterModelBySlug, GetShelterPrice) + `SheltersController` (`GET /api/v1/shelters`, `GET /api/v1/shelters/{slug}`, `GET /api/v1/shelters/{slug}/price?lengthCm=`) ; calcul de prix délégué à `ShelterPriceCalculator` (source unique, L-004) ; bornes/alignement longueur → `BusinessRuleException` (422, jamais 500) ; `[AllowAnonymous]` sur les 3 endpoints. Tests : 5 unit (GetShelterPriceQueryHandlerTests) + 7 IT (SheltersEndpointTests, `[Collection("Integration")]`). Gates : `dotnet build` 0 erreur ✅ · `dotnet test` 374/0 (289 unit + 85 IT) ✅. **Écarts à confier au mentor en fin d'épic :** (a) `WebAppFactory` ne sème QUE `IdentitySeeder` (pas `ShelterModelSeeder`/`ProductSeeder`) → un IT doit semer ses propres lignes via `IApplicationDbContext` directement ; (b) les collections EF **owned** (`ShelterModel.Dimensions` via `OwnsMany`) ne sont **pas `Include`-ables** — EF lève à l'exécution ; la projection doit charger l'entité racine et accéder aux owned-props directement, sans `.Include(...)`. **Curseur → EPIC 9.3** (boutique/product-detail + configurateur de dimensions front). Découpage restant : 9.3 configurateur → 9.4 panier/parcours/commande → 9.5 admin référentiel.) *Historique :* **EPIC 9.1 FAIT** (commit `fdf60ea`) : entité `ShelterModel` paramétrique + owned `ShelterModelDimension` (Width/ClearHeight) ; `ShelterPriceCalculator` (n_arches=(longueur−min)/pas, L-007) ; `ShelterPricing.DefaultPricePerArchCents=15000` ; config EF + migration `AddShelterModels` (`20260618191207`) appliquée LocalDB ; `ShelterModelSeeder` idempotent (4 modèles) ; L-010 affûtée (lookup tolérant aux doublons). Gates : `dotnet test` 284/78 ✅ · round-trip live ✅ · `solid-review` 0 violation ✅. *Historique :* **EPIC 15 — SPIKE champ d'adresse (US-15.1) TERMINÉ** sur la
  branche `docs/epic-15-address-spike` : `docs/spikes/epic-15-address-field-spike.md`. Décision
  utilisateur = **un seul champ « n°+rue » + auto-rempli ÉDITABLE** (lecture seule du `.docx` écartée
  — un mauvais code postal doit rester corrigeable) + tokens WCAG 1.3.5 + voie serveur B1.
  Implémentation US-15.2/15.3 **différée**. **Curseur → EPIC 9** (catalogue par dimensions) ou refonte
  US-15.2/15.3 à la demande. *Historique :* **EPIC 12 (Phase 2 — contraste formulaires/focus) TERMINÉ** sur la branche `fix/epic-12-p2-contrast-badge-tab` : 4 surfaces blanc-sur-rouge/ambre en échec AA **en thème sombre** corrigées **au niveau jeton** (badge « Ajusté serré » `/mesurer`, onglet profil actif, créneau d'installation sélectionné, numéro d'étape `/mesurer` courant), gardées par 3 e2e dual-thème non vacueux prouvés en échec→succès ; revue indépendante **APPROVE WITH NITS** (nit factorisation traité ; elle a trouvé 2 des 4 surfaces, intégrées au même diff) ; **L-033** capturée. **Curseur → EPIC 15** (spike champ d'adresse), puis 9 → 10 → 13 → 14 → 11 → 8 → 7.) *Historique :* **Épic G MERGÉ vers `master` — PR #39 (`ed5f82e`), CI verte, prod auto-déployée.** L'unique e2e rouge était un flake d'hydratation SSR pré-existant (`fill('77')` one-shot sur `#civicNumber` avant hydratation du CVA), PAS une régression G — fix `fill` dans `expect().toPass()` (commit `1eb1321`), **L-012 affûtée**, CI repassée verte avant merge. **Curseur → Épic H (déploiement backend MANUEL utilisateur : `docs/deployment.md` §4.2 — `az login` + `gh secret set AZURE_CREDENTIALS` + FQDN dans `environment.prod.ts`).** Le front A→G est livré ET en ligne ; il ne reste que le provisioning Azure backend, manuel.**) — *Historique :* **Épic F « Accès invité (compte express silencieux) » MERGÉ vers `master`** (PR #38 `e536cd2`, merge `--admin`). Compte express passwordless trouvé-ou-créé par courriel (`AppUser.IsExpress`, rôle `Customer`, migration `AddIsExpressToAppUser`) ; `[AllowAnonymous]` sur les 3 POST de création seulement ; composant front `app-guest-contact` + retrait `authGuard`/redirections `/auth`. Revue APPROVE WITH NITS (sécurité passwordless+sibling-actions OK ; non-régression Ontario→pass intacte). Leçons **L-028/L-029** capturées. **⚠️ Déploiement prod EN ATTENTE** : 3 gates code verts mais `Build & Deploy` (SWA) bloqué par un **rate-limit MCR externe** (429) — re-run `gh run rerun 27580207119 --failed` quand MCR récupère. **Prochain épic : G — catalogue marques/modèles + fit `/mesurer`.** *Historique :* **Épic E MERGÉ** (PR #37 `233a388`), CI verte, prod auto-déployée. E1 déjà en place (verrou e2e), E2 alerte adresse 1re connexion scopée profil sans migration. Revue APPROVE WITH NITS. *Historique :* **Épic D MERGÉ** (PR #34 `9c91254`) : Autofill robuste + carte mesurer centrée + zone 100 km non bloquante + 2 choix d'adresse connecté (pastille/parcours anonyme). Gate Sonar passé en 2 itérations (duplication câblage → `AddressFormController` auto-injecté ; label input avatar). Leçons **L-026/L-027** ajoutées, **L-016** affûtée. Bug-09 (badge sombre) loggé en suivi. *Historique :* **Épic C MERGÉ** (PR #33 `a4ad2d2`) : bascule de langue exploitable en local (`dev:i18n` + dégradation a11y du bouton EN via flag `environment.localized` SSR-safe), Major staging `angular.json localize` attrapé en revue + corrigé, leçons **L-024/L-025** capturées ; CI verte, prod auto-déployée. **Plan G enrichi** sur demande utilisateur : G3 = carte `/mesurer` trop petite + proposition d'abris-qui-entrent à déboguer (`probleme contraste (2).docx`) ; D4/D6 = **2 choix d'adresse pour utilisateur connecté** (pastille profil + centrage auto carte, OU bascule parcours anonyme). **Prochaine sous-tâche : Épic D — système d'adresse unifié.** *Historique :* **Épic B curseur MERGÉ** (PR #32). *Historique :* **Flake CI `webgl.util` éliminé** (PR #28) + **déploiement backend PRÉPARÉ** (PR #29) : Container Apps + Azure SQL S0, Dockerfile/workflow/migration-opt-in/doc §4.2, inerte jusqu'au provisioning ; L-022 capturée ; reste = l'utilisateur déroule `docs/deployment.md` §4.2. *Historique :* **Flake-01 corrigé et MERGÉ** — `role="status"` sur la grille de chargement de la Boutique, corrige `aria-prohibited-attr` intermittent ; PR #25 `288df31`, branche supprimée, CI verte ; revue APPROVE ; garde de test non vacueuse `loading()`+axe ; `npm test` **203**, e2e Boutique 8/8 ; flake CI `webgl.util` re-run vert L-017. **Il ne reste plus aucune sous-tâche actionnable — seul le redéploiement prod manuel subsiste.** *Historique :* **F2-D corrigé** — carte `/mesurer` dessinable ; cause réelle = geoman IIFE lisant `globalThis.L` + `addInitHook` post-construction, **PAS** l'interop CJS/ESM supposée ; fix `globalThis.L = L` + import geoman avant `L.map(...)` ; revue APPROVE WITH NITS, gates verts dont **e2e 73/0**, **L-021** capturée / L-019 corrigée ; branche `fix/f2d-mesurer-geoman-draw` en attente de PR→CI→merge. *Historique :* **Suivis F2-B + F2-C livrés et MERGÉS** — **PR #21 `5665677`**, branche supprimée, CI verte — revue APPROVE WITH NITS, gates verts, L-019/L-020. Défaut **F2-D geoman** (mode carte non dessinable, préexistant Épic D) **découvert** par la garde de test F2-B et tracké comme suivi ouvert + **Flake-01 catalog**. *Historique :* **🎉 PROGRAMME TERMINÉ — Épic F mergé vers `master`, PR #19 `62e6981`, branche supprimée, CI verte** ; flake `webgl.util.spec` re-run vert (L-017) ; aucune sous-tâche restante ; suivis optionnels = redéploiement prod + F2-B/F2-C mineurs. *Historique :* **Épic F prêt à merger** : F1→F4 + F2-A + revue indépendante APPROVE WITH NITS (nits clôturés `5adb1f3`) + L-018 ; reste PR `docs/program-wrapup`→`master` + CI. *Historique :* **Épic F F1→F4 TOUS faits** : règle motion-a11y + hook ; passe heuristique F2 (F2-A majeur focus admin) ; README état final ; CI sanity → `build:prod` (budgets) ; **réparation hors-programme** `cf0bf64` hero statique/fusion auth/deep-link/contraste. Reste : **frontière F = revue → PR → master**, à confirmer avec l'utilisateur ; décider F2-A avant/après PR. Branche `docs/program-wrapup`, 4 commits devant `master`.)

### Épic C — ✅ TERMINÉ (branche `feat/address-split-autocomplete`)

| Sous-tâche | État | Commits |
|-----------|------|---------|
| C1 — Split Address VO + migration + validateur canonique | ✅ revu | `5e97441`, `983ed1f`, `d37556a` |
| C2 — Proxy Places (Photon/Radar/Google + rate limiter) | ✅ revu | `53175c3` |
| C3 — Autocomplete accessible (APG combobox) + 4 formulaires | ✅ revu | `8183d46`, flake fix `6e23b48` |
| Fold-in marque/modèle + exclusion ShelterLogic | ✅ revu | `80b0c37` |
| Correctifs de revue indépendante (Photon province, focusout, track) | ✅ | `2c963f7` |

**Revue indépendante** : REQUEST CHANGES → corrigée (1 bloquant Photon province nom→code 2 lettres + 2 mineurs). **Leçons capturées par le `mentor` : L-011, L-012, L-013** (+ L-004/L-010 affûtées). Migrations `SplitAddressCivicNumber` (`20260613033910`) et `AddBookingBrandModel` (`20260613051112`) appliquées sur LocalDB + eyeballées (L-001).
**Gates finales** : `dotnet test` 226 ✅ · `npm test` 120 (zéro axe) ✅ · `npm run e2e` 56 (0 flake, ×3) ✅ · `build:prod` ✅. Gate Sonar : duplication new-code ramenée 6.8 %→<3 % via `AddressAutofillService` (`b3e8f27`).
**Statut git :** **PR #16 mergée vers `master` ✅ (`36097f4`)**, branche supprimée. Docs retournées : `board.md`, `product-backlog.md` (US-2.8/2.9), README roadmap, `wcag-2.2-audit.md` (1.3.5 / 4.1.2).

## Épic B — TERMINÉ (branche `feat/missing-sections`)

| Sous-tâche | État | Notes |
|-----------|------|-------|
| B1 — Pages légales | ✅ | `47d306f` |
| B2 — Réinitialisation mot de passe bout-en-bout | ✅ | `52a9064`, `c54782d` |
| B3 — Admin réservations / locations / utilisateurs | ✅ | `3e4c174` `f43fbe4` `bcaa95e` `d268b82` |
| #11 — Correctifs de revue B1/B2 | ✅ | `478a711` (parité mot de passe L-004, IT en CI L-005, a11y) |
| B4 — Heuristiques (H1 langue, H5 disponibilité, FAQ) | ✅ | revue indépendante APPROVE WITH NITS ; leçon L-010 |

Docs retournées : README roadmap, `docs/agile/board.md` (section clôture Épic B),
`docs/ux/heuristic-evaluation.md` (H1–H10 tous remédiés). **PR #15 mergée vers `master` ✅ (`7ccec41`).**

---

## ✅ EPIC 9 — Catalogue par dimensions configurables : TERMINÉ (2026-06-19)

Branche `feat/epic-9-dimension-catalog`. Boucle complète *architecte → développeur → revue
indépendante `code-reviewer` à la frontière d'épic → mentor*.

| Sous-tâche | Statut |
|-----------|--------|
| 9.1 — Entité `ShelterModel` paramétrique + owned `ShelterModelDimension` + migration `AddShelterModels` + `ShelterPriceCalculator` + `ShelterModelSeeder` (4 modèles) | ✅ |
| 9.2 — API catalogue paramétrique : `GET /api/v1/shelters`, `GET /api/v1/shelters/{slug}`, `GET /api/v1/shelters/{slug}/price` (`[AllowAnonymous]`) + 5 unit + 7 IT | ✅ |
| 9.3 — Configurateur front : pages `modeles/:category` + `configurer/:slug`, composant `dimension-configurator` (radiogroups APG L-015, range, prix debounce, `aria-live` L-027), CTA boutique/product-detail, i18n symétrique | ✅ |
| 9.4 — Panier/commande : état panier abri configuré, ajout au panier, `shelterLines` sans prix dans `PlaceOrder` (serveur recalcule, L-004), ligne de commande `OrderLineShelterConfig`, migration `AddOrderLineShelterConfig`, IT invité+auth+422, e2e | ✅ |
| 9.5 — Admin CRUD `ShelterModel` : domaine `Reconfigure()` + entité régulière `ShelterModelDimension` + migration `20260619150301` (Up/Down vides, schéma inchangé côté SQL Server) + commands Create/Update/Delete (`[Authorize(Policy="AdminOnly")]`) + page admin `shelter-models` (signals, OnPush, reactive forms, a11y, i18n 58 ids) + tuile dashboard | ✅ |
| Correctif Minor revue : `CategoryId` exposé sur `ShelterModelDetailDto`, bind par id dans `UpdateShelterModelCommandHandler`, suppression `categoryIdForName` | ✅ |

**Décision de modélisation** : modèle paramétrique « B » — largeur/hauteur fixes par catégorie, longueur configurable par pas de 4 pi, prix calculé serveur (n_arches). `ShelterModelDimension` : entité régulière (non owned après migration) avec clé primaire autonome.

**Déviation EF owned → entité régulière** : la migration `20260619150301_ShelterModelDimensionRegularEntity` a des corps `Up()`/`Down()` vides — le schéma SQL Server était déjà en place. C'est attendu et documenté en L-035.

**Revue indépendante** : `code-reviewer` **APPROVE WITH NITS** — 1 Minor corrigé (CategoryId), nits clôturés dans le commit feat(catalog).

**Leçons capturées** : **L-035** (entité owned→régulière = migration vide côté SQL Server, snapshot mis à jour) · **L-036** (bind par id pour les relations de catégorie, exposer CategoryId sur le DTO) · **L-024 affûtée** (bind par id vs nom pour les relations de catégorie).

**Gates** :
- `dotnet build` 0 erreur · `dotnet test` 344 unit + 95 IT, 0 échec · filtré Shelters : 81 unit + 16 IT, 0 échec, incl. assertion `CategoryId`
- `npm run build` (typecheck) OK · `npm test` shelter-models + dimension-configurator **19/19**
- `npm run e2e admin-shelter-models` **5/5** · axe dual-thème zéro violation WCAG AA
- i18n : 58 ids `admin.shelterModels.*` symétriques fr/en (L-018)
- Migration `20260619150301` Up/Down vides (schéma SQL Server inchangé) — attendu (L-035)
- Sécurité : POST/PUT/DELETE `[Authorize(Policy="AdminOnly")]`, GET restés `[AllowAnonymous]` · IT couvre 401/403/422/409/404

**Statut git** : **PR #45 MERGÉE vers `master` (`44997586`), CI verte (Backend 1m05s / Frontend+i18n+axe 8m25s / Build & Deploy 1m37s / SonarCloud 32s), branche `feat/epic-9-dimension-catalog` supprimée.** Prod auto-déployée. **Prochain : EPIC 10** (suggestion d'abris intelligente).

---

## Suite du programme

- **Épic C** — ✅ TERMINÉ — Adresse structurée + autocomplétion accessible (proxy Photon/Radar/Google) — `feat/address-split-autocomplete`
- **Épic D** — Outil `/mesurer` parking + suggestions (Leaflet + turf) — `feat/mesurer-parking` — **← prochain**
- **Épic E** — Redesign v2 (tokens v2, GSAP hero, three.js viewer) — `feat/redesign-v2` — *après D*
- **Épic F** — Wrap-up docs/process — `docs/program-wrapup`

Détail complet de chaque épic : voir le fichier de plan référencé en tête.
Règle git de fin d'épic : revue → commit → PR → revue CI → merge/push `master`.
