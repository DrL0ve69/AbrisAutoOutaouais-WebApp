{
  "//": "Project-scoped MCP servers for AbrisTempo Local, committed so the repo is self-contained:",
  "//2": "anyone who clones it gets the Angular tooling without manual `claude mcp add`. It is",
  "//3": "auto-approved via enabledMcpjsonServers in .claude/settings.json (no trust prompt).",
  "mcpServers": {
    "angular-cli": {
      "command": "npx",
      "args": ["-y", "@angular/cli", "mcp"]
    }
  }
}
