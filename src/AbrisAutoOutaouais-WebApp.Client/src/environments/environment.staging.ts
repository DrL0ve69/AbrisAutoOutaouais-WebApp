export const environment = {
  production: false,
  apiUrl: 'https://staging-api.abristempo.com/api/v1',
  // Build optimisé servi bilingue comme la prod : la bascule de langue est active.
  localized: true,
} as const;
